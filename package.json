{
  "name": "beerstore-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Run frontend and backend together",
  "scripts": {
    "build": "npm --prefix frontend run build",
    "start": "npm run build && npm --prefix backend run start",
    "serve": "npm --prefix backend run start",
    "dev": "concurrently -k -n backend,frontend -c green,cyan \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
